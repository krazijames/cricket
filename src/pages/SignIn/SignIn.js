import React from 'react';
import { withStyles } from '@material-ui/core/styles';

import { Layout } from 'components';

const SignIn = withStyles((theme) => ({}))(({ classes }) => {
  return <Layout>Sign In</Layout>;
});

export default SignIn;
